#include<bits/stdc++.h>

using namespace std;

    int main() {
    double x, y, result;
    cin >> x >> y;
    result = x / y;
    cout << fixed << setprecision(3);
    cout << result << " km/l " << endl;

    return 0;

    }
